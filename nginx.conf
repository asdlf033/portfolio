# AWS nginx 설정 파일
# 이 파일을 /etc/nginx/sites-available/srdomain.org 에 복사하고
# /etc/nginx/sites-enabled/srdomain.org 로 심볼릭 링크를 생성하세요

server {
    listen 80;
    listen [::]:80;
    server_name srdomain.org www.srdomain.org;
    
    # HTTP에서 HTTPS로 리다이렉트 (SSL 인증서가 있는 경우)
    # return 301 https://$server_name$request_uri;
    
    # 정적 파일 디렉토리
    root /var/www/html;
    index index.html;
    
    # Gzip 압축 활성화
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/xml+rss
        application/json;
    
    # 정적 파일 캐싱
    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
    
    # SPA 라우팅을 위한 fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 보안 헤더
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;
}

# HTTPS 설정 (SSL 인증서가 있는 경우)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name srdomain.org www.srdomain.org;
#     
#     ssl_certificate /path/to/your/certificate.crt;
#     ssl_certificate_key /path/to/your/private.key;
#     
#     root /var/www/html;
#     index index.html;
#     
#     # 나머지 설정은 위와 동일
# }
